import { EpisodenumberPipe } from './episodenumber.pipe';

describe('EpisodenumberPipe', () => {
  it('create an instance', () => {
    const pipe = new EpisodenumberPipe();
    expect(pipe).toBeTruthy();
  });
});

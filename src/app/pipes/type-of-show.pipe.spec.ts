import { TypeOfShowPipe } from './type-of-show.pipe';

describe('TypeOfShowPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeOfShowPipe();
    expect(pipe).toBeTruthy();
  });
});

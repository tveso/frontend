import {ExtraMdTagsPipe} from './extra-md-tags.pipe';

describe('ExtraMdTagsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraMdTagsPipe();
    expect(pipe).toBeTruthy();
  });
});
